class Email {
  constructor(
    id,
    emailFrom,
    emailTitle,
    emailBody
  ) {
    this.id = id;
    this.emailFrom = emailFrom;
    this.emailTitle = emailTitle;
    this.emailBody = emailBody;
  }
}

export default Email;
